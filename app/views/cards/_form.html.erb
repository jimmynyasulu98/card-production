<%= form_with(model: card) do |form| %>
  <% if card.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(card.errors.count, "error") %> prohibited this card from being saved:</h2>

      <ul>
        <% card.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :cover_id, style: "display: block" %>
    <%= form.text_field :cover_id %>
  </div>

  <div>
    <%= form.label :location_id, style: "display: block" %>
    <%= form.text_field :location_id %>
  </div>

  <div>
    <%= form.label :date_joined, style: "display: block" %>
    <%= form.date_field :date_joined %>
  </div>

  <div>
    <%= form.label :date_of_expiry, style: "display: block" %>
    <%= form.date_field :date_of_expiry %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
